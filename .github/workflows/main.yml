on:
    push:
        branches:
            - master



            



jobs:
  build:
    runs-on: ubuntu-latest


    steps:
      - uses: actions/checkout@v3
      - name: "docker login"
        run: docker login --name ${{ secrets.DOCKER_USERNAME}} --password ${{secrets.DOCKER_PASSWORD}}
    
      
